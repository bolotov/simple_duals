## Notes to self to better remember and recall it later

On **MacOS** just press:
`Control` + `Command` + `Spacebar` together,
and type Epsilon in the search bar

On **Windows** (I was told that it works like this):
Alt and type `03b5` on a numpad, that is epsilon, right?

`Alt` + `numpad` combinations used to work on **Windows** time ago
haven't checked this specific code

There is a whole discussion on **LaTeX** epsilon and how to better
write it in documents and papers:

https://tex.stackexchange.com/questions/98013/varepsilon-vs-epsilon

----
## R-methods in code

"Magic" methods like `__rpow__` or `__radd__` are *right-hand operands*
and not about being *real* or *rational*
